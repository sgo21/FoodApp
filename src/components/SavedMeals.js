import React, { useState } from "react";

const SavedMeals = () => {

    
//   const [show, setShow] = useState(false);
//   const [saved, setSaved] = useState(false);
//   const { label, image, url, ingredients, calories, dietLabels, healthLabels} = recipe.recipe;

//   return (
//     <div className="recipe">
//       <h2>{label}</h2>
//       <img src={image} alt={label} />
//       <button id="save-meal-button" onClick={() => setSaved(!saved)}>Save to Favorites ❤️</button>
//       {saved && <p>Recipe added to favorites!</p>}
//       <a href={url} target="_blank" rel="noopener noreferrer">
//         View Recipe
//       </a>
//       <h3> {Math.round(calories)} Calories </h3>
//       <h3> {dietLabels} </h3>
//       <h3> {healthLabels} </h3>
//       <br/>
//       <button onClick={() => setShow(!show)}>Ingredients</button>
//       {show && <RecipeDetails ingredients={ingredients} />}
//     </div>
//   );
};

export default SavedMeals;